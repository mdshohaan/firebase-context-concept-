
const Profile = () => {
  // const {user} = useContext(authContext)
  // console.log(user);
  // if(!user){
  //   return <Navigate to="/signin"></Navigate>
  // }
  return (
    <div>
      I am Profile
    </div>
  );
};

export default Profile;